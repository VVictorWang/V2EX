<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.victor.v2ex">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Themes.HotActivity" />
        <activity android:name=".Themes.Neweset" />
        <activity
            android:name=".Reply.ContentActivity"
            android:parentActivityName=".Themes.HotActivity" />
        <activity android:name=".Node.NodeActivity" />
        <activity android:name=".TabActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Node.AllNodeActivity" />
        <activity android:name=".Member.MemberActivity" />
        <activity android:name=".Image.BigPhoto"></activity>
    </application>

</manifest>